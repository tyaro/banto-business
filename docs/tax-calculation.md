# 税計算仕様

**状態：未確定（Phase 1 で確定させる）**

`TODO` が残っている項目は実装前に必ず埋めること。空欄のまま推測で実装しない。

---

## 1. 前提

| 項目 | 値 |
|---|---|
| 事業者区分 | TODO（適格請求書発行事業者 / 免税事業者） |
| 登録番号 | TODO（`T` + 13桁） |
| 経理方式 | TODO（税抜経理 / 税込経理） |
| 請求書の表示方式 | TODO（税抜表示 / 税込表示） |
| 課税方式 | TODO（原則課税 / 簡易課税） |

> 免税事業者の場合、登録番号欄と税額欄の扱いが変わる。Phase 1 で確定させること。

---

## 2. 適格請求書の必須記載事項

請求書PDFに以下を必ず出力する。

- [ ] 適格請求書発行事業者の氏名または名称
- [ ] 登録番号（T + 13桁）
- [ ] 取引年月日
- [ ] 取引内容（軽減税率対象品目である旨を含む）
- [ ] 税率ごとに区分して合計した対価の額
- [ ] 税率ごとの消費税額
- [ ] 税率ごとの適用税率
- [ ] 交付を受ける事業者の氏名または名称

### レイアウト上の注意

- 軽減税率対象がある場合、対象品目に記号（※等）を付し、記号の意味を欄外に注記する
- 税率区分が1種類のみの場合も、税率と税額を明示する

---

## 3. 税率区分

| コード | 税率 | 用途 |
|---|---|---|
| `STANDARD_10` | 10% | 標準税率。開発費・設計費・交通費・宿泊費・部材費等 |
| `REDUCED_8` | 8% | 軽減税率。飲食料品等 |
| `EXEMPT` | — | 非課税 |
| `OUT_OF_SCOPE` | — | 不課税 |

### 経費分類ごとの既定税区分

| 経費分類 | 既定 | 備考 |
|---|---|---|
| 交通費 | `STANDARD_10` | 国内。国際線は免税のため要個別判断 |
| 宿泊費 | `STANDARD_10` | 宿泊税・入湯税は不課税のため分離が必要か Phase 1 で判断 |
| 送料 | `STANDARD_10` | |
| 部材費 | `STANDARD_10` | |
| 消耗品 | `STANDARD_10` | |
| 外注費 | `STANDARD_10` | |
| その他 | TODO | |

**既定値であり、行ごとに変更可能とする。**

---

## 4. 端数処理

### 4.1 原則

**端数処理は1つの適格請求書につき、税率ごとに1回。**

明細行ごとの個別端数処理は行わない。

### 4.2 計算順序

```
1. 明細行を税率区分ごとにグループ化
2. 各グループ内で対価の額を合計          ← ここまで端数処理なし
3. グループごとに 合計額 × 税率 を計算
4. グループごとに1回だけ端数処理          ← 端数処理はここのみ
5. 各グループの税額を合算して請求書合計税額とする
```

### 4.3 端数処理方向

| 項目 | 値 |
|---|---|
| 消費税額の端数処理 | TODO（切捨て / 四捨五入 / 切上げ） |
| 処理桁 | 1円未満 |

- 設定値として保持し、Invoice に処理方向をスナップショットする
- 一度発行した請求書の端数処理方向は、設定変更後も遡って変わらないこと

### 4.4 明細行単価の端数

明細行の `数量 × 単価` で端数が出る場合の扱い：

TODO（Phase 1 で決定。案：単価は整数円のみ許容し、端数を発生させない）

---

## 5. 実装インターフェース（案）

```rust
// src-tauri/src/domain/tax.rs

pub enum TaxCategory {
    Standard10,
    Reduced8,
    Exempt,
    OutOfScope,
}

pub enum RoundingMode {
    Floor,
    Round,
    Ceil,
}

pub struct TaxLine {
    pub amount: i64,          // 円。税抜
    pub category: TaxCategory,
}

pub struct TaxGroupResult {
    pub category: TaxCategory,
    pub rate_bp: u32,         // basis point (10% = 1000)
    pub taxable_amount: i64,  // 税率区分ごとの対価合計
    pub tax_amount: i64,      // 端数処理後の税額
}

pub struct TaxSummary {
    pub groups: Vec<TaxGroupResult>,
    pub total_taxable: i64,
    pub total_tax: i64,
    pub total_amount: i64,    // 税込合計
}

pub fn calculate_tax(
    lines: &[TaxLine],
    rounding: RoundingMode,
) -> TaxSummary;
```

**この関数は純粋関数とし、DB・Tauri に依存させない。**

---

## 6. テストケース表

実装時に以下を全てテストする。`期待値` は Phase 1 で確定させる。

| # | ケース | 入力 | 期待値 |
|---|---|---|---|
| T-01 | 10%のみ・端数なし | 100,000円 × 10% | 税額 10,000 |
| T-02 | 10%のみ・端数あり（切捨て） | 33,333円 × 10% | TODO |
| T-03 | 10%のみ・端数あり（四捨五入） | 33,335円 × 10% | TODO |
| T-04 | 8%のみ | TODO | TODO |
| T-05 | 10% + 8% 混在 | TODO | TODO |
| T-06 | 10% + 不課税 混在 | TODO | TODO |
| T-07 | 全税率混在 | TODO | TODO |
| T-08 | **明細行ごとに端数処理した場合と結果が異なることの検証** | 33,333円 × 3行 | 行ごと処理: 9,999 / 区分ごと処理: TODO |
| T-09 | 明細0件 | `[]` | 全て 0 |
| T-10 | 金額0円の明細を含む | TODO | TODO |
| T-11 | マイナス金額（値引き行） | TODO | TODO |
| T-12 | 端数処理方向のスナップショット | 設定変更後も既発行分が不変 | 不変 |

**T-08 は特に重要。** 端数処理の位置を誤った実装をリグレッションで検出するための基準ケースとする。

---

## 7. 未決事項

Phase 1 で決定すること。

- [ ] 事業者区分・登録番号
- [ ] 税抜経理 / 税込経理
- [ ] 請求書の税抜表示 / 税込表示
- [ ] 端数処理方向
- [ ] 明細行単価の端数の扱い
- [ ] 宿泊税・入湯税の分離要否
- [ ] 値引き行（マイナス金額）を許容するか
- [ ] 立替経費の請求時の税区分の引き継ぎ方
- [ ] 源泉徴収が発生する契約の有無（発生する場合、請求書への記載要否）

---

## 8. 参照

- 国税庁「適格請求書等保存方式（インボイス制度）」
- 適格請求書発行事業者公表サイト（登録番号の確認）

> 制度の解釈に迷う点は税理士・所轄税務署に確認すること。このドキュメントは実装仕様であり、税務判断の根拠ではない。
