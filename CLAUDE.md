# CLAUDE.md

Banto Business リポジトリの常時適用ルール。
**このファイルの内容は、個別の指示より優先される。矛盾する指示を受けた場合は作業を止めて確認すること。**

詳細な設計背景は `docs/plan.md`、開発手順の詳細は `AGENTS.md` を参照。

---

## 0. このプロジェクトは何か

個人事業の案件管理デスクトップアプリ。
顧客 → 案件 → 工数/経費 → 案件原価 → 請求 → 入金 → 案件採算 を一元管理する。

**会計ソフトではない。** 複式簿記・仕訳・決算・確定申告・給与計算は範囲外。

---

## 1. 絶対規約（違反はレビューで差し戻す）

### 1.1 金額

- 金額は **必ず INTEGER（円単位）**。`REAL` / `f64` / 浮動小数点は使用禁止
- Rust側は `i64`、TypeScript側は `number`（整数として扱う）
- 率（税率・粗利率等）は basis point（1/10000）または分子分母で保持。小数で持たない
- 端数処理は必ず方向（切捨て/四捨五入/切上げ）と桁を明示。暗黙のデフォルトに依存しない

### 1.2 原価レートのスナップショット

- `WorkLog` の内部原価は **レートマスタを参照しない**
- 記録時点の単価を `applied_rate` として **行に焼き付ける**
- レートマスタは新規入力時のデフォルト値の供給源としてのみ使う
- 理由：後から時給設定を変えたときに過去案件の採算が遡って書き換わると、採算指標が信用できなくなる

同じ原則を Invoice にも適用する。**確定した Invoice は確定時点の値をスナップショットとして保持し、元の WorkLog / Expense の変更は波及させない。**

### 1.3 Invoice は Project と 1:1 にしない

```
Invoice (customer_id を持つ)
  └─ InvoiceLine
        └─ project_id
```

- 1請求書に複数案件（締日まとめ請求）
- 1案件に複数請求書（着手金 / 中間金 / 検収）

の両方が実務で発生する。`Invoice.project_id` を作らないこと。

### 1.4 Payment は Invoice と 1:1 にしない

```
Invoice ─┐
         ├─ PaymentAllocation（充当額・差額理由コード）
Payment ─┘
```

一部入金・まとめ入金・振込手数料の先方差引・源泉徴収が発生するため N:M。

### 1.5 Overdue は状態として持たない

`status` カラムに `Overdue` を作らない。以下の導出条件で都度評価する。

```
Overdue := (due_date < today) AND (残額 > 0) AND (status != Cancelled)
```

理由：状態として持つと日次バッチが必要になり、アプリを毎日起動しないローカルファースト構成で実態とずれる。

### 1.6 証憑の正本は会計ソフト側

Attachments に保存する領収書は **案件紐付け用の参照コピー**。正本ではない。
電子帳簿保存法の要件（検索要件・訂正削除履歴）を満たす実装をこちらに持ち込まないこと。

### 1.7 適格請求書

請求書PDFは適格請求書の記載要件を満たすこと。端数処理は **1請求書につき税率ごとに1回**。
明細行ごとの個別端数処理は行わない。詳細は `docs/tax-calculation.md`。

---

## 2. 責務境界

| リポジトリ         | 責務                                                                                                                  |
| ------------------ | --------------------------------------------------------------------------------------------------------------------- |
| `banto`            | フレームワーク：CRUD / Grid / Forms / Chart / Storage / Auth / RBAC / Report / Attachments / Backup / Server / 共通UI |
| `banto-industrial` | 産業ドメイン：FA / PLC / SCADA / Tag Server                                                                           |
| `banto-business`   | **このリポジトリ**：Customer / Project / WorkLog / Trip / Expense / Invoice / Payment / Profitability                 |

### やってはいけないこと

- **Banto が提供する機能を再実装しない。** Grid / Form / Report / Attachments 等は Banto のものを使う
- **Banto 本体の設計規約を複製・改変してこちらに持ち込まない**
- **Business固有機能を Banto へ逆流させない**
- Banto に機能が足りない場合、勝手に回避実装を作り込む前に `docs/banto-feedback.md` に記録し、確認を取る

---

## 3. Banto 依存のバージョン管理

**方式：同梱（vendoring）＋派生元コミット固定**（2026-08-19 Phase 0 決定。決定理由は `docs/template-origin.md`）

- Banto の `crates/*` / `packages/*` は **このリポジトリに同梱**し、`path` 依存 / `workspace:*` で参照する。git タグ参照の薄い構成にはしない
  - 理由：Banto 側 `docs/publishing.md` がタグ参照の対象を `@banto/*` / `banto-*` に限定し、アプリ本体（`admin-template-core` / `src-tauri`）は「リポジトリそのものをクローンして使う前提」と明記しているため
- 固定単位は**タグではなく派生元コミット SHA**。現在の固定先は `docs/template-origin.md`「現在の派生状態」に記録する。`main` 追従（無検討の一括取り込み）は禁止
- 上流を取り込む場合は、差分を確認 → 取り込む/見送る/保留を分類 → 動作確認 → `docs/template-origin.md` のマージ判断ログに記録、の順で行い、理由をコミットメッセージに残す
- 同梱した Banto のコード（`crates/*` / `packages/*` / `apps/admin-template` のうち Banto 由来部分）を Business 都合で**書き換えない**。必要が生じたら `docs/banto-feedback.md` に記録して確認を取る（第2章）

---

## 4. 技術構成

- Tauri v2 / Rust / SvelteKit / Svelte 5 / TypeScript
- DB: SQLite（初期版）。将来 PostgreSQL 移行の可能性があるため、SQLite固有機能への依存は最小化する
- 日付は ISO 8601、DBには UTC ではなく **ローカル日付（JST）の DATE / TEXT** で保持する（業務日付であり時刻の意味を持たないため）

---

## 5. DB Migration

- マイグレーションは前方向のみ。適用済みマイグレーションを編集しない
- 破壊的変更は新規マイグレーションで行う
- ファイル名は `NNNN_snake_case_description.sql`
- **金額カラムは必ず `INTEGER NOT NULL`**
- スナップショット系カラム（`applied_rate`, 確定Invoice明細等）に外部キー制約で参照整合を強制しない（マスタ変更で過去が壊れるため）

---

## 6. テスト要件

以下は必ずテストを書く。

- 案件採算計算（工数原価 / 経費集計 / 粗利 / 実質時間単価2種）
- 税計算（税率区分別集計・端数処理）— `docs/tax-calculation.md` のケース表を網羅
- Payment 消込（一部入金 / まとめ入金 / 手数料差引 / 過入金）
- Overdue 導出条件の境界（期限当日 / 残額0 / Cancelled）
- Trip 一括生成（生成件数と内訳）

UI のスナップショットテストは必須としない。**金額が絡むロジックは必ずテストする。**

---

## 7. 作業の進め方

- Phase を飛ばさない。現在の Phase は `docs/plan.md` 第18章を参照
- Phase 1（ドメイン設計）が確定するまで、Phase 2以降のテーブルを先行実装しない
- Banto の標準手順から外れる実装をした場合、必ず `docs/banto-feedback.md` に記録する
- 不明点は推測で実装せず、質問する。特に **税計算・採算計算・消込ロジック**は推測禁止

---

## 8. 公開リポジトリでの取り扱い

このリポジトリは **public**。同梱している Banto も public（MIT）。

- **実データをコミットしない。** 業務DB（`*.sqlite3`）・バックアップ（`backups/`）・領収書（`attachments/`）は `.gitignore` 済み。無視設定を外さない
- **事業者情報をリポジトリに書かない。** 適格請求書の発行者情報（登録番号 `T`+13桁 / 屋号 / 住所 / 振込先口座）は設定データとして DB / 設定画面で保持する。ドキュメント・テストフィクスチャ・シードに実値を書かない（`docs/tax-calculation.md` の該当欄はサンプル値で埋める）
- 顧客名・案件名は、テスト・サンプルデータでは**架空のもの**を使う
- CI ワークフローに `secrets` を追加する場合、fork からの PR では渡らない前提で設計する

---

## 9. 実装前チェックリスト

金額・請求・入金に関わるコードを書く前に確認：

- [ ] 金額は INTEGER（円）か
- [ ] レート・単価をスナップショットしているか
- [ ] Invoice を Project に直接紐づけていないか
- [ ] Payment を Invoice に 1:1 で紐づけていないか
- [ ] Overdue をカラムとして作っていないか
- [ ] 端数処理の方向と桁を明示しているか
- [ ] Banto が既に提供している機能を再実装していないか
